"""Vendored primitives from scqc_pipeline (see VENDORING.md).

These modules are COPIED (not imported) from the sibling scqc_pipeline project
and evolve independently here. Only import paths, provenance keys, and the uns
key were adapted to scpilot; the logic is upstream's. Keep scpilot-native code
out of this subpackage so the vendoring boundary (and re-sync diffs) stay clean.
"""
