"""Tier1 broad consensus(major_cell_type) + Tier3 fine(fine/facs_style_label, evidence는 annotation_tree) — scpilot plan B8/B13.

TODO: 미구현(skeleton). 계약: AnnData in → 요약 dict out(`scpilot.schemas`),
mutating tool은 `.uns["scpilot"]` provenance + 체크포인트 기록.
"""
